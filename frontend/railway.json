{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s dist/knowsphere/browser --listen tcp://0.0.0.0:$PORT"
  }
}